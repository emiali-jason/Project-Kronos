# KRONOS Python Package

`src/kronos` is the Python implementation package for the KRONOS platform.

Package boundaries mirror the approved platform domains. Architecture documents remain authoritative, and implementation must not change domain ownership. Cross-domain communication must use approved contracts or events.

This foundation contains no trading logic.
