# WO-11 prospective Position / Paper Observation / Lifecycle V1

**Status: Approved policy; engineering candidate pending qualification.**

The consolidated final engineering order authorizes implementation and isolated
qualification. It does not authorize any production activation, runtime load,
commit or publication. Existing operational admission guards remain authoritative.
Historical WO-11 and WO-15/16/17 records, owners and deep links retain their
original authority. Prospective ownership belongs exclusively to WO-11 under
`KRONOS-INTRADAY-PROSPECTIVE-PROGRAMME-V2`.

## Frozen decisions

All N1–N15 decisions are approved subject to the Sponsor supersessions. Both
PAPER_POSITION and PAPER_OBSERVATION model exactly one lot. The WO-10 selected
quantity is upstream context only. Observation is counterfactual non-exposure.
No PAPER price or quantity input, REST pricing, broker fill or LIVE activation
is permitted. Future LIVE remains SPONSOR_ATTESTED_LIVE_FACTS and is not
commissioned by this implementation.

The exact current WO10_SELECTED_TRADE_HANDOFF_V1 graph, Native selection,
WO-09 pointer, geometry, session and exact Future bind explicit Sponsor arming.
An arming authorization is session-bound and may outlive its original quote.
Before entry it cannot survive cancellation, governed upstream supersession or contract/roll
termination. Post-entry analytical reassessment is NOT_COMMISSIONED_V1. A new action identity cannot bypass the opportunity/expression
claim shared by Position and Observation.

Completed 5M aligned PULLBACK timing uses canonical underlying authority for
NSE and native contract authority for MCX. The subsequent exact-Future eligible
WebSocket LTP crosses the immutable mapped Entry inclusively. No retest,
historical fill, dynamic basis remap or quote-only qualification is added.
Entry must precede 15:00 IST NSE / 23:00 IST MCX and the actual session close.

`WO11_WEBSOCKET_OBSERVATION_LATENESS_V1` admits the exact interval
`0 <= received_at - observed_at <= 5.000000 seconds`. No rounding occurs.
Timezone awareness, causal prerequisites, exact contract/session, unrecovered
continuous observations and ordering remain mandatory. Lateness does not supply
sequence authority. Conflicting equal-time prices without governed sequence
cannot establish ordered consequences. Late observations remain evidence and
cannot create entry, exit, Stop, Target, manual-close pricing or MFE/MAE.

Model prices are exactly ProviderMarketTick.last_price. Receipt time is latency
evidence only. Missing bid/ask is not manufactured with FullQuote. Gaps remain
immutable; recovery requires a fresh baseline and does not reconstruct missed
entry, exit, extrema or order. Restore does not restore cached ticks as current
or authenticate Provider. Observation never becomes Portfolio exposure.

Stop/Target use inclusive directional thresholds and actual qualifying LTP.
After Entry the only trading exit reasons are STOP_LOSS, TARGET and
SPONSOR_EXIT. A Sponsor EXIT PAPER or STOP OBSERVATION request requires the
first subsequent eligible price. Before Entry those actions are non-trade
terminal dispositions. The original thesis invalidation is context only, never a model-close event. Final session/earlier contract termination cannot use an earlier cached
mark or a next-day price. Missing lawful terminal pricing remains unavailable.

MFE/MAE use eligible post-entry directional deviations, starting at zero, with
sample identities and gap coverage. Initial model risk must be positive for R.
Gross monetary result uses one exact lot's governed units/economics; missing
MCX economics remains unavailable. PAPER and counterfactual labels remain
separate. WO-12 receives immutable source lineage and one opportunity denominator.
WO-13 notification delivery and downstream Journal/Portfolio/Reports are absent.

## Post-entry analytical invalidation — ADR-0046

POST_ENTRY_ANALYTICAL_INVALIDATION_AUTOMATIC_EXIT = NOT_COMMISSIONED_V1.
The immutable WO-10 invalidation definition is retained as ORIGINAL THESIS
INVALIDATION, with THESIS_DEFINITION_CONTEXT_ONLY role. No new reassessment
producer exists. Stop traversal, Target traversal, LTP, 5M evidence and the
original definition cannot manufacture an analytical event.

Only the already-governed upstream currentness rules can terminate pre-entry
eligibility. They do not trigger post-entry analytical closure. Commissioned V1
trading exit authority is exactly STOP_LOSS, TARGET and SPONSOR_EXIT. Final
session/contract boundaries and fail-closed unavailable/ambiguous outcomes are
separate `terminal_status` values, never exit reasons.

Each immutable V1 record retains NOT_COMMISSIONED_V1. WO-12 explicitly carries
both that capability state and the original thesis definition. Absence of a
reassessment is never NOT_INVALIDATED. POST_ENTRY_ANALYTICAL_INVALIDATION_V2
is reserved only; a future version must not reinterpret or rewrite V1 records.

## Implementation and qualification boundaries

The candidate separates record/eligibility policy, pure state transitions,
append-only store, exact WO-10 intake, existing-source timing adaptation,
shared-hub application, and Browser projection. The new namespace is
`prospective-v2-wo11-lifecycle`; historical stores are not migrated.

Trade Candidates holds Sponsor lifecycle actions. Active and Closed separate
Position and counterfactual Observation. The primary navigation remains the
existing five tabs. GET projections do not acquire prices or execute lifecycle
operations. The runtime service retains operational admission guards; source composition
has no uncommissioned analytical-source dependency.

This engineering candidate requires isolated qualification. Test results and source inventories
are recorded in the external engineering report, not asserted as policy.
No release or production acceptance is claimed.


## Contract and restoration composition

The runtime composes the existing active-derivative resolution source separately
from analytical authority. MCX timing uses the exact serialized active_binding
identity, expiry uses the retained DOMAIN-008 eligibility boundary, and a governed
new exact contract/roll terminates the old track without carrying it into the new
contract. A missing contract source does not invent a roll or analytical event.
The current pointer, all predecessor records, embedded sources and immutable
entry/exit observations are verified on restoration. No production operation is
performed during engineering qualification.
